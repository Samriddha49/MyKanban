# Used by docker-compose.yml for the backend service.
# Generate a real random secret before any real deployment, e.g.: openssl rand -base64 48
JWT_SECRET=change-this-to-a-long-random-secret-value-min-32-bytes
